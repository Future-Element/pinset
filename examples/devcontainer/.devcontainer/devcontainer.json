{
  "name": "Pinset",
  "build": {
    "dockerfile": "Dockerfile",
    "args": {
        "PINSET_VERSION": "2.2.0"
    }
  },
  "postCreateCommand": "pinset install --locked",
  "remoteUser": "vscode"
}
